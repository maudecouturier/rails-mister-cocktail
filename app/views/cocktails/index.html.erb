<h1>Mister cocktails'</h1>
<div>
  <ul>
    <% @cocktails.each do |cocktail| %>
      <li><%= cocktail.name %> | <%= link_to 'See recipe', cocktail_path(cocktail) %></li>
    <% end %>
  </ul>
</div>

<p><%= link_to 'Add a new recipe', new_cocktail_path %></p>
